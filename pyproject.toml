[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Base inicial do sistema web FisioBase para um consultório de fisioterapia"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "flask-login>=0.6.3",
    "flask-sqlalchemy>=3.1.1",
    "gunicorn>=26.2.0",
]
